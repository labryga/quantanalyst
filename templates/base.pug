{% load staticfiles %}
!!! 5

html
	{% include "head.pug" %}

	body
		{% include "header.pug" %}
		
		main
			div(class="grid_row")	
				// navigation desktop/tablet => 600px
				div(id="menu") 
					a(id="menu_mobile", style="display: none")= "Menu"
					{% block navigation %}
					{% endblock navigation %}


				div(id="inhalt", class="content")
					{% block content %}
					{% endblock content %}

		{% include "footer.pug" %}

		script(src="{% static 'js/production/quantanalyst.js' %}")
